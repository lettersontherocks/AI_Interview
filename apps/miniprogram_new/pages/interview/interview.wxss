/* pages/interview/interview.wxss */
page {
  width: 100%;
  height: 100%;
  overflow-x: hidden;
}

.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  width: 100%;
  background: linear-gradient(180deg, #fff9f0 0%, #fdf2f8 100%);
  overflow-x: hidden;
  position: relative;
}

/* 进度条 */
.progress-bar {
  width: 100%;
  height: 6rpx;
  background: #e0e0e0;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #ff9a9e 0%, #fad0c4 100%);
  transition: width 0.3s;
}

.progress-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15rpx 20rpx;
  background: white;
}

.progress-text {
  font-size: 26rpx;
  color: #999;
  flex: 1;
}

.view-mode-switch {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 8rpx 16rpx;
  background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
  border-radius: 20rpx;
  color: white;
  font-size: 22rpx;
  box-shadow: 0 2rpx 8rpx rgba(255, 154, 158, 0.3);
  transition: all 0.2s;
}

.view-mode-switch:active {
  transform: scale(0.95);
  opacity: 0.9;
}

.switch-icon {
  font-size: 24rpx;
}

.switch-text {
  font-weight: 500;
}

/* 聊天区域 */
.chat-area {
  flex: 1;
  padding: 20rpx;
  padding-bottom: 180rpx;  /* 留出足够空间给固定的输入框（约120rpx高度 + 60rpx缓冲） */
  overflow-y: scroll;
  -webkit-overflow-scrolling: touch;  /* iOS平滑滚动 */
}

.message-item {
  display: flex;
  margin-bottom: 30rpx;
  animation: fadeIn 0.3s;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10rpx); }
  to { opacity: 1; transform: translateY(0); }
}

.message-item.interviewer {
  justify-content: flex-start;
}

.message-item.candidate {
  justify-content: flex-end;
}

.message-avatar {
  width: 70rpx;
  height: 70rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #ffffff 0%, #f0f0f0 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  flex-shrink: 0;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
  border: 3rpx solid #fff;
}

.message-item.interviewer .message-avatar {
  background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
}

.message-item.candidate .message-avatar {
  background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);
}

.message-item.interviewer .message-avatar {
  margin-right: 15rpx;
}

.message-item.candidate .message-avatar {
  margin-left: 15rpx;
  order: 2;
}

.message-content {
  max-width: 70%;
}

.message-text {
  background: white;
  padding: 20rpx 25rpx;
  border-radius: 20rpx 20rpx 20rpx 4rpx;
  font-size: 28rpx;
  line-height: 1.6;
  color: #333;
  word-wrap: break-word;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  position: relative;
  animation: messageSlideIn 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1rpx solid rgba(0, 0, 0, 0.03);
}

@keyframes messageSlideIn {
  from {
    opacity: 0;
    transform: translateX(-15rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.message-item.candidate .message-text {
  background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);
  color: #5a4a42;
  border-radius: 20rpx 20rpx 4rpx 20rpx;
  box-shadow: 0 4rpx 16rpx rgba(161, 196, 253, 0.25);
  animation: messageSlideInRight 0.3s ease;
}

@keyframes messageSlideInRight {
  from {
    opacity: 0;
    transform: translateX(15rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.message-feedback {
  margin-top: 15rpx;
  padding: 15rpx;
  background: rgba(255, 154, 158, 0.1);
  border-radius: 10rpx;
  font-size: 24rpx;
}

.score {
  color: #ff6b6b;
  font-weight: bold;
  margin-bottom: 8rpx;
}

.hint {
  color: #666;
}

/* 输入loading */
.typing-indicator {
  display: flex;
  gap: 8rpx;
  padding: 20rpx 25rpx;
  background: white;
  border-radius: 15rpx;
}

.typing-indicator span {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #ff9a9e;
  animation: typing 1.4s infinite;
}

.typing-indicator span:nth-child(2) {
  animation-delay: 0.2s;
}

.typing-indicator span:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes typing {
  0%, 60%, 100% { opacity: 0.3; }
  30% { opacity: 1; }
}

/* 输入区域 */
.input-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 10rpx 15rpx;
  padding-bottom: calc(10rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.9);
  border-top: 1rpx solid #ffecd2;
  display: flex;
  gap: 8rpx;
  align-items: flex-end;
  z-index: 100;
  box-sizing: border-box;
}

/* 模式切换按钮 */
.mode-toggle {
  width: 44rpx;
  height: 44rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #fff9f0;
  border-radius: 50%;
  transition: all 0.2s;
  margin-bottom: 4rpx;
  box-shadow: 0 2rpx 8rpx rgba(255, 154, 158, 0.1);
  border: 1rpx solid #ffecd2;
}

.mode-toggle:active {
  background: #ffecd2;
  transform: scale(0.95);
}

.mode-icon {
  font-size: 32rpx;
}

/* 文字输入模式 */
.text-input-wrapper {
  flex: 1;
  display: flex;
  gap: 8rpx;
  align-items: flex-end;
  min-width: 0;
  background: #fff9f0;
  border-radius: 20rpx;
  padding: 6rpx 12rpx;
  box-shadow: 0 1rpx 2rpx rgba(255, 154, 158, 0.05);
}

.answer-input {
  flex: 1;
  min-height: 36rpx;
  max-height: 150rpx;
  padding: 0;
  background: transparent;
  border-radius: 0;
  font-size: 28rpx;
  width: 100%;
  box-sizing: border-box;
  border: none;
  line-height: 1.5;
}

.answer-input:focus {
  background: transparent;
}

.input-placeholder {
  color: #ccc;
  font-size: 26rpx;
}

.send-btn {
  width: 56rpx;
  height: 56rpx;
  flex-shrink: 0;
  border-radius: 50%;
  background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  margin-bottom: 2rpx;
  box-shadow: 0 4rpx 12rpx rgba(255, 154, 158, 0.4);
}

.send-icon {
  font-size: 32rpx;
  font-weight: bold;
  line-height: 1;
  transform: translateX(2rpx);
}

.send-btn:active {
  transform: scale(0.92);
  box-shadow: 0 2rpx 8rpx rgba(255, 154, 158, 0.3);
}

.send-btn-disabled {
  background: linear-gradient(135deg, #e0e0e0 0%, #d0d0d0 100%);
  box-shadow: none;
  opacity: 0.6;
}

.send-btn-disabled .send-icon {
  color: #999;
}

.send-btn-disabled:active {
  transform: none;
}

/* 语音输入模式 */
.voice-input-wrapper {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 15rpx;
}

.voice-status {
  text-align: center;
  padding: 10rpx;
  min-height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 10rpx;
}

.voice-hint {
  font-size: 26rpx;
  color: #999;
}

.recording-animation {
  display: flex;
  gap: 8rpx;
  align-items: center;
}

.wave {
  width: 6rpx;
  height: 30rpx;
  background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
  border-radius: 3rpx;
  animation: wave-animation 1s infinite ease-in-out;
}

.wave:nth-child(1) {
  animation-delay: 0s;
}

.wave:nth-child(2) {
  animation-delay: 0.2s;
}

.wave:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes wave-animation {
  0%, 100% {
    height: 30rpx;
  }
  50% {
    height: 60rpx;
  }
}

.recording-time {
  font-size: 32rpx;
  color: #ff6b6b;
  font-weight: bold;
  margin-top: 5rpx;
}

.record-btn {
  width: 100%;
  padding: 20rpx;
  background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
  color: white;
  border-radius: 50rpx;
  font-size: 32rpx;
  font-weight: bold;
  border: none;
  box-shadow: 0 4rpx 12rpx rgba(255, 154, 158, 0.3);
  transition: all 0.3s;
}

.record-btn:active {
  transform: scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(255, 154, 158, 0.4);
}

.record-btn[disabled] {
  opacity: 0.5;
  background: #ccc;
  box-shadow: none;
}

/* 完成区域 */
.finish-area {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(180deg, #fff9f0 0%, white 100%);
  padding: 40rpx 30rpx;
  text-align: center;
  border-top: 1rpx solid #ffecd2;
}

.finish-icon {
  font-size: 100rpx;
  margin-bottom: 20rpx;
}

.finish-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #ff6b6b;
  margin-bottom: 10rpx;
}

.finish-subtitle {
  font-size: 26rpx;
  color: #a8a29e;
  margin-bottom: 30rpx;
}

.view-report-btn {
  width: 80%;
  margin: 0 auto;
}

/* 导入沉浸模式样式 */
@import "./immersive.wxss";
